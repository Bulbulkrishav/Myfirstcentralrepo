pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                deleteDir()
                checkout scm
            }
        }
        stage('listing') {
            steps {
                echo "hello world"
            }
        }
    }
}