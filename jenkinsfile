pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                deleteDir()
                checkout scm
                sh "ls -ltr"
            }
        }
        stage('listing') {
            steps {
                echo "hello world"
            }
        }
    }
}